//Creating the Note classmodel.Note = new DataClass("Notes");//Add Note attributes.model.Note.ID = new Attribute("storage", "long", "key auto");model.Note.title = new Attribute("storage", "string", "btree");model.Note.body = new Attribute("storage", "string", "btree");model.Note.createDate = new Attribute("storage", "date");model.Note.owner = new Attribute("relatedEntity", "User", "User"); // relation to the User classmodel.Note.contact = new Attribute("relatedEntity", "Contact", "Contact"); // relation to the Contact classmodel.Note.lead = new Attribute("relatedEntity", "Lead", "Lead"); // relation to the Lead classmodel.Note.account = new Attribute("relatedEntity", "Account", "Account"); // relation to the Account class//Eventsmodel.Activity.events = {};model.Activity.events.onInit = function() {	var myCurrentUser = currentUser(), // we get the user of the current session.		myUser = ds.User.find("ID = :1", myCurrentUser.ID);			if ((myCurrentUser !== null) && (myUser !== null)) {//if a user is logged in.				this.owner = myUser;	}		this.createDate = new Date();	}; //end - onInit().